# Aleth — OpenEnv Citation Verification Benchmark
# All dependencies for server + inference

pydantic>=2.0.0
python-dotenv>=1.0.0
typing-extensions>=4.5.0

# LLM inference
openai>=1.0.0

# HTTP client (inference.py talks to the Aleth server)
requests>=2.28.0

# FastAPI server (OpenEnv submission)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# OpenEnv SDK (install if provided by hackathon; optional)
# openenv-core
